$if(title)$
  \title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$

$if(subtitle)$
  \subtitle{$subtitle$}
$endif$

\author{${ by-author:_authors.tex()[\and ] }}

$if(date)$
  \date{\normalsize$date$}
$else$
  \date{\normalsize\today}
$endif$
